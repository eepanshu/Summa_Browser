# Render.yaml - Render deployment configuration
services:
  - type: web
    name: summabrowser-api
    env: python
    buildCommand: "pip install -r summary/requirements-simple.txt"
    startCommand: "cd summary && python app-minimal.py"
    plan: free
    envVars:
      - key: FLASK_ENV
        value: production
      - key: PORT
        value: "10000"
